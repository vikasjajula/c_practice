#include<stdio.h>
void main()
{
	int k[23];
	char v[12];
	printf("the size of data type int is %d bytes\n",sizeof(int));
	printf("the size of data type char is %d bytes\n",sizeof(char));
	printf("we can allocate %d locations in the variable c\n",sizeof(k));
	printf("we can allocate %d locations in the variable v\n", sizeof(v));
	
}
